<div>
  <h1>
    Currently signed in as: <%= current_user.name || current_user.email %>.
    <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
  </h1>
  <h3>
    Redirect to <%= link_to "GoPlan Web", ENV['GOPLAN_WEB_BASE_URL'], target: "_blank" %>
  </h3>
</div>
